# Do not run on push
trigger: none

extends:
  template: azure-pipelines.release-template.yml
  parameters:
      skipTests: true
      shouldPublish: true
      dualPublish: false
      registry: docker.io/getporterci
      publishExamples: false
